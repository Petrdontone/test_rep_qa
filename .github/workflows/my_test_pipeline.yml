name: My first CI
author: Peter Yasnetsov

description: My first ci and la la la

on:
 pull_request:
   branches:
     - "main"

jobs:
 simple_job:
   runs-on: ubuntu-latest
   steps:
      - uses: actions/checkout@v4
      - name: echo hello world
        run: echo Hello, world!
      - name: date show
        run: echo $(date)
    

 
